#Calculate the average age of students
1student = 20 //years
            # it has syntax error, we can not start variable name with special character and years is undefined variable.

student2 = 25
student_#3 = 23    # Here, after the # all the character are considered as a comments.
x = 19
s-3 = 21           # we can not assign value like this.
averageStudentAge = (x+s-3((1student+student2)) + student_#3)//5
            # it will not give answer because of the 1student and student_#3. Because it has syntax error.

print(averageStudentAge)
            # if we do not get answer from upper, so we will also not get answer from this command.